# ---------- Database (PostgreSQL with pgvector, async driver) ----------
DATABASE_URL=postgresql+asyncpg://hintro:hintro@localhost:5433/hintro

# ---------- Auth ----------
JWT_SECRET=change-me-to-a-long-random-string
JWT_ALGORITHM=HS256
JWT_EXPIRES_MINUTES=10080

# ---------- LLM providers ----------
# Primary. Free key: https://aistudio.google.com/apikey
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
# Fallback. Free key: https://console.groq.com/keys
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile
EMBEDDING_MODEL=models/gemini-embedding-001

# ---------- Redis (cache + rate limit; optional, degrades gracefully) ----------
REDIS_URL=redis://localhost:6379

# ---------- Integrations ----------
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
DISCORD_WEBHOOK_URL=

# ---------- Scheduler ----------
CRON_SECRET=change-me-cron-secret

# ---------- App ----------
ENVIRONMENT=development
PORT=8000
CORS_ORIGIN=*
PUBLIC_API_URL=http://localhost:8000

# ---------- Candidate (for /api/evaluation) ----------
CANDIDATE_NAME=Aman Kumar
CANDIDATE_EMAIL=aman0611kumar@gmail.com
REPOSITORY_URL=
DEPLOYED_URL=
